namespace noop.controlFlow;

import java.lang.NumberFormatException;

doc """
Examples of throwing and catching Exceptions in Noop.

All exceptions are unchecked.
"""
class Exceptions() {
  Int parseInt(String str) throws OhNoException {
    try {
      java.lang.Integer.parseInt(str);
    } catch {
      // case NumberFormatException:
        // throw OhNoException("String #{str} wasn't a number");
    }
  }
}
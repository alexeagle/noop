namespace arithmetic;

import noop.Application;
import noop.Console;

doc """
Two things about Arithmetic:
 * This class demonstrates some basic arithmetic operations
 * It converts an Int to a String automatically

 @author Alex Eagle (alexeagle@google.com)
"""
class Arithmetic(noop.Console console) implements Application {
  Int main(List args) {
    Int i = 1;
    Int j = 2;
    Int k = i + j;

    console.println(k);
    return 0;
  }
}

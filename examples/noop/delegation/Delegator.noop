//
// Demonstration class that declares another instance as a delegate and 
// absorbs (inherits) its contract, delegating all contracted methods to 
// the delegate
//
namespace delegation;

class Delegator(delegate Delegate d) implements Application {

  Int main(List args) {
    aMethod();
  }

}
